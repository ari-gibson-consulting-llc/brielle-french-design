---
import type { PortfolioEntryCarousels } from "../../lib/getContent";
import Carousel from "../Carousel.astro";
type Props = Pick<
  PortfolioEntryCarousels["props"],
  "firstCarousel" | "secondCarousel" | "thirdCarousel" | "fourthCarousel"
> & {
  slidesToShow: Array<number | undefined>;
};
const {
  firstCarousel,
  secondCarousel,
  thirdCarousel,
  fourthCarousel,
  slidesToShow,
} = Astro.props;
---

{
  [firstCarousel, secondCarousel, thirdCarousel, fourthCarousel].map(
    (carousel, i) =>
      carousel && <Carousel images={carousel} slidesToShow={slidesToShow[i]} />,
  )
}
